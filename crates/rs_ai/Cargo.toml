[package]
name = "rs_ai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "(UNSTABLE; in active development) Fluent top-level API for the Rusty AI SDK — unified interface to Claude, ChatGPT, Gemini, xAI, Cloudflare, and OpenAI-compatible endpoints. Builder pattern with env-var fallback, vision, streaming, caching, TTS/STT."

[dependencies]
rs_ai_core = { workspace = true }
rs_ai_providers = { workspace = true, features = ["claude", "chatgpt", "gemini", "openai-compatible", "xai", "cloudflare"] }
futures = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
