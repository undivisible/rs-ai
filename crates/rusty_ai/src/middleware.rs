//! Middleware types for intercepting generation requests.
//!
//! The core [`Middleware`] and [`MiddlewareNext`] types are defined in
//! [`crate::model`] and re-exported from the crate root.

// This module exists as a namespace placeholder. The primary middleware
// types live in `model.rs` because the linter/project convention co-locates
// them with the `LanguageModel` trait.
